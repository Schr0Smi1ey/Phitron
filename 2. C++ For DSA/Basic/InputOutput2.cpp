#include<bits/stdc++.h>
#include<string.h>
#include<algorithm>
#include<stdlib.h>
#include<math.h>
// with typing (std::) in every line we can simply use (using namespaces std;)
using namespace std;
int main()
{
    int a,b;
    cout<<"input a & b : ";
    cin>>a>>b; // taking input from terminal using (cin)...in C++ we don't to use format specifier like C.
    cout<<"a = "<<a; // printing value of a..here we don't need to use format specifier.....just type the variable which we need to print with double quotation
    cout<<"\n";
    cout<<"b = "<<b<<endl;
    cout<<"a + b : "<<a+b<<endl;
    cout<<"a : " <<a<<" "<<"b : "<<b; // to print with the space we need to print space manaully
    
}
#include<bits/stdc++.h>
using namespace std;
int main()
{
    float x;
    cin>>x;
    cout<<fixed<<setprecision(1)<<x; // this will print 2 digit after the float
    // this (fixed and setprecision) under #inclue<iomanip> (input output manipulation)library
}